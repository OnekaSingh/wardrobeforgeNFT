import { ConnectWithUIOptions, ShowUIPromiEvents, Sign7702AuthorizationRequest, Sign7702AuthorizationResponse, Send7702TransactionRequest, Send7702TransactionResponse } from '@magic-sdk/types';
import { BaseModule } from './base-module';
type ShowUiConfig = {
    onramperParams?: {
        [key: string]: string;
    };
};
export declare class WalletModule extends BaseModule {
    connectWithUI(options?: ConnectWithUIOptions): import("../util").PromiEvent<string[], {
        'id-token-created': (params: {
            idToken: string;
        }) => void;
    } & {
        wallet_connected: () => void;
        wallet_rejected: () => void;
        web3modal_selected: () => void;
    } & {
        done: (result: string[]) => void;
        error: (reason: any) => void;
        settled: () => void;
        "closed-by-user": () => void;
    }>;
    showUI(config?: ShowUiConfig): import("../util").PromiEvent<boolean, ShowUIPromiEvents & {
        done: (result: boolean) => void;
        error: (reason: any) => void;
        settled: () => void;
        "closed-by-user": () => void;
    }>;
    showAddress(): import("../util").PromiEvent<boolean, {
        done: (result: boolean) => void;
        error: (reason: any) => void;
        settled: () => void;
        "closed-by-user": () => void;
    }>;
    showSendTokensUI(): import("../util").PromiEvent<boolean, {
        done: (result: boolean) => void;
        error: (reason: any) => void;
        settled: () => void;
        "closed-by-user": () => void;
    }>;
    showOnRamp(): import("../util").PromiEvent<boolean, {
        done: (result: boolean) => void;
        error: (reason: any) => void;
        settled: () => void;
        "closed-by-user": () => void;
    }>;
    showNFTs(): import("../util").PromiEvent<boolean, {
        done: (result: boolean) => void;
        error: (reason: any) => void;
        settled: () => void;
        "closed-by-user": () => void;
    }>;
    showBalances(): import("../util").PromiEvent<boolean, {
        done: (result: boolean) => void;
        error: (reason: any) => void;
        settled: () => void;
        "closed-by-user": () => void;
    }>;
    /**
     * Sign an EIP-7702 authorization to delegate the EOA to a smart contract implementation.
     * This enables account abstraction features for externally owned accounts.
     *
     * @param params - The authorization parameters including contractAddress, chainId, and optional nonce
     * @returns Promise resolving to the signed authorization with signature components (v, r, s)
     *
     * @example
     * ```typescript
     * const authorization = await magic.wallet.sign7702Authorization({
     *   contractAddress: '0x000000004F43C49e93C970E84001853a70923B03',
     *   chainId: 1,
     *   nonce: 0
     * });
     * ```
     */
    sign7702Authorization(params: Sign7702AuthorizationRequest): import("../util").PromiEvent<Sign7702AuthorizationResponse, {
        done: (result: Sign7702AuthorizationResponse) => void;
        error: (reason: any) => void;
        settled: () => void;
        "closed-by-user": () => void;
    }>;
    /**
     * Send an EIP-7702 (Type-4) transaction with an authorization list.
     * This allows executing transactions with delegated smart contract implementations.
     *
     * @param params - The transaction parameters including to, value, data, gas settings, and authorizationList
     * @returns Promise resolving to the transaction hash
     *
     * @example
     * ```typescript
     * // First, sign the authorization
     * const auth = await magic.wallet.sign7702Authorization({
     *   contractAddress: '0x000000004F43C49e93C970E84001853a70923B03',
     *   chainId: 1,
     * });
     *
     * // Then, send the transaction with the authorization
     * const { transactionHash } = await magic.wallet.send7702Transaction({
     *   to: '0x...',
     *   data: '0x...',
     *   authorizationList: [auth],
     * });
     * ```
     */
    send7702Transaction(params: Send7702TransactionRequest): import("../util").PromiEvent<Send7702TransactionResponse, {
        done: (result: Send7702TransactionResponse) => void;
        error: (reason: any) => void;
        settled: () => void;
        "closed-by-user": () => void;
    }>;
}
export {};
