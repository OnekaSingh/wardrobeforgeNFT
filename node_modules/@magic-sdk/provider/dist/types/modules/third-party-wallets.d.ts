import { JsonRpcRequestPayload, MagicThirdPartyWalletRequest, ThirdPartyWalletEvents } from '@magic-sdk/types';
import { BaseModule } from './base-module';
import { PromiEvent } from '../util';
export declare class ThirdPartyWalletsModule extends BaseModule {
    eventListeners: {
        event: ThirdPartyWalletEvents;
        callback: (payloadId: string) => Promise<void>;
    }[];
    enabledWallets: Record<string, boolean>;
    isConnected: boolean;
    resetThirdPartyWalletState(): void;
    requestOverride(payload: Partial<JsonRpcRequestPayload>): PromiEvent<any, {
        done: (result: any) => void;
        error: (reason: any) => void;
        settled: () => void;
        "closed-by-user": () => void;
    }>;
    private isLoggedIn;
    private getInfo;
    private logout;
    private magicWidgetRequest;
    private magicWidgetLogout;
    handleIframeThirdPartyWalletRequest(event: MagicThirdPartyWalletRequest): void;
}
